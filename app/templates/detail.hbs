<div class="container">
  <!--  Outer row  -->
  <div class="row">
    <div class="section col s12 m9 l10">
      <div id="download" class="row scrollspy">
        <div class="row">
          <div class="col s8">
            <h2 class="col s12 header">{{model.title}}</h2>
          </div>
          <div class="col s4">
            {{view "select" action="changeStatus" content=status value=selectedStatus class="browser-default fix-select"}}
          </div>
        </div>
          <p class="caption col s12">{{model.description}}</p>
          <div class="col s12 m6">
            <p class="promo-caption"> Desenvolvedor Resposável: {{developer.full_name}}</p>
          </div>
          <div class="col s12 m6">
            <p class="promo-caption">Prioridade: {{priority}}</p>
          </div>
      </div>
    </div>
  </div>
</div>
